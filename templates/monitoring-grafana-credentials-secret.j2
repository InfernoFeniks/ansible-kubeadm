apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: monitoring
data:
  user: {{ ( kubeadm_grafana_user | b64encode ) | default( 'admin' | b64encode )}}
  password: {{ ( kubeadm_grafana_password | b64encode ) | default( 'admin' | b64encode ) }}